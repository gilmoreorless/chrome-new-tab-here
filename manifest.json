{
	"name": "Open New Tab Here",
	"version": "0.1.0",
	"description": "Keyboard shortcut to open a new tab next to the current one",
	"manifest_version": 2,
	"icons": {},
	"background": {
		"scripts": ["listener.js"]
	},
	"commands": {
		"new-tab-here": {
			"suggested_key": {
				"default": "Alt+T",
				"mac": "MacCtrl+T"
			},
			"description": "Open a new tab next to the current one"
		}
	},

	"permissions": ["tabs"]
}
